services:
  worker:
    build: .
    command: ["bin/claude-inbox-worker"]
    user: "${DOCKER_UID}:${DOCKER_GID}"
    env_file: .env
    environment:
      CLAUDE_INBOX: /data/inbox
      CLAUDE_INBOX_WORKDIR: /workdir
      HOME: /home/claude-inbox
      CLAUDE_MODEL: ${CLAUDE_MODEL:-}
    volumes:
      - ${CLAUDE_INBOX:-~/.claude-inbox}:/data/inbox
      - ${HOME}/.claude:/home/claude-inbox/.claude
      - ${HOME}/.claude.json:/home/claude-inbox/.claude.json
      - ${CLAUDE_INBOX_WORKDIR:-/workspaces/claude-inbox}:/workdir
      - ${HOME}/.config/nlm:/home/claude-inbox/.config/nlm
    restart: unless-stopped
    deploy:
      replicas: ${WORKERS:-1}

  bridge-telegram:
    build: .
    command: ["bin/claude-inbox-bridge-telegram", "--skip-pending"]
    user: "${DOCKER_UID}:${DOCKER_GID}"
    env_file: .env
    environment:
      CLAUDE_INBOX: /data/inbox
      HOME: /home/claude-inbox
    volumes:
      - ${CLAUDE_INBOX:-~/.claude-inbox}:/data/inbox
    restart: unless-stopped
